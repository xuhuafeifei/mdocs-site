# 所见皆文件

mdocs 的设计参考 Linux 的一切接文件理念，左侧目录树在底层存储时，都是以文件形式表示。

目录由 `desc.md` 文件描述，文档本身即是文档。

所有的物理数据皆以文件形式存储在文件系统中，而 mdocs 所使用的则是由本地 SQLite 维护的文件逻辑关系。

每次创建文档或目录（实际也是创建 `desc.md` 文件），都会在 `documents` 表中落记录。

目录树由服务端根据文档路径动态构建，目录之间的层级关系隐含在路径片段中，没有单独的父子关系表。