# 所见皆文件

mdocs的设计参考 Linux 的一切接文件理念, 左侧目录树在底层存储时，都是以文件形式表示。

目录，有`desc.md`文件描述，文档本身就是由文档描述。

所有的物理数据皆以文件形式存储在文件系统中，而`mdocs`所使用的则是由本地sqlite维护的文件逻辑关系。

用户每次创建一个目录(dir), 亦或是文件(file), 都会在sqlite中落表

由 sqlite 维护文件目录之间的父子关系