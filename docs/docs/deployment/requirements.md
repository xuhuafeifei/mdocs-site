# 环境要求

## 最低配置

| 资源 | 要求 |
|------|------|
| 操作系统 | Linux / macOS / Windows |
| CPU | 1 核 |
| 内存 | 512 MB |
| 磁盘 | 100 MB（不含文档存储） |

## 依赖

- **无外部依赖**：mdocs 单二进制文件运行
- 无需安装数据库、缓存、消息队列等中间件

## 存储

- 文档以 Markdown 文件存储在文件系统
- 元数据（索引、权限等）存储在 SQLite 数据库
- 备份只需复制整个目录即可
